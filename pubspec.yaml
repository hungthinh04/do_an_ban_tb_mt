name: do_an_ban_mt
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  carousel_slider: ^5.0.0
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
